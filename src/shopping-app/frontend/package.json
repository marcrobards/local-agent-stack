{
  "name": "shopping-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/index.tsx --outdir dist --minify --splitting --target browser"
  },
  "dependencies": {
    "hono": "^4",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
